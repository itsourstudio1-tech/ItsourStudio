import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.itsourstudio.admin',
  appName: 'ItsOurStudioAdmin',
  webDir: 'dist'
};

export default config;
